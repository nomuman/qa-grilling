# Authentication and accounts

Activate for signup, login, session, identity, roles, linking, logout, and deletion.

Consider interrupted signup, verification expiry, duplicate accounts, multi-device login, reset races, SSO mapping, linking and unlinking, logout with in-flight work, deletion with background work, export and deletion semantics, account switching with cached private data, and role or permission changes during a session.
