# APIs and distributed systems

Activate for service boundaries, queues, jobs, webhooks, and remote APIs.

Consider client timeout versus server success, idempotency, retries at multiple layers, request ordering, eventual consistency, pagination stability, duplicate or reordered webhooks, rate limits, schema evolution, partial dependency failure, clock skew, correlation IDs, backward compatibility, and exactly-once assumptions that the architecture does not guarantee.
