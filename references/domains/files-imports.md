# Files and imports

Activate for uploads, archives, imports, and exports.

Consider extension versus actual content, unsupported or mislabeled MIME types, corrupt archives, decompression bombs, huge file counts, duplicate filenames, Unicode and path semantics, partial import, rollback and retry, imported ownership and permissions, metadata privacy, and export round-trip fidelity.
