# Offline and sync

Activate when local work can outlive connectivity or synchronize across devices.

Define the source of truth, outbox semantics, conflict resolution, same-item edits, delete-versus-edit behavior, stale resurrection prevention, retry ordering, queued-operation dependencies, authentication expiry, account switching with queued writes, delayed media availability, migration with unsynced work, cursor invalidation, and clock-based conflict risks.

Write explicit deletion and ownership invariants.
