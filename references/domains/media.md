# Media

Activate for camera, image, video, or audio capture and processing.

Consider large files, codec and container differences, metadata orientation, corrupt or truncated files, duration and resolution extremes, decode and encode memory pressure, temporary storage, repeated compression, background interruption, upload resume, local source deletion, preview mismatch, audio route changes, permission changes, hardware contention, thermal and battery impact, metadata privacy, and partial server-side processing.
